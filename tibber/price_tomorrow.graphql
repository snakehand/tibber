query PriceTomorrow ($id: ID!) {
  viewer {
    home (id: $id) {
      currentSubscription {
        priceInfo {
          tomorrow {
            total
            energy
            tax
            startsAt
            currency
            level
          }
        }
      }
    }
  }
}
