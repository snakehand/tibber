query PriceTomorrow ($id: ID!) {
  viewer {
    home (id: $id) {
      currentSubscription {
        priceInfo(resolution: QUARTER_HOURLY) {
          tomorrow {
            total
            energy
            tax
            startsAt
            currency
            level
          }
        }
      }
    }
  }
}
