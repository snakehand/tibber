query PriceToday ($id: ID!) {
  viewer {
    home (id: $id) {
      currentSubscription {
        priceInfo (resolution: QUARTER_HOURLY) {
          today {
            total
            energy
            tax
            startsAt
            currency
            level
          }
        }
      }
    }
  }
}
